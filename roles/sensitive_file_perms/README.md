# Role: sensitive_file_perms

Enforces root ownership and secure octal file permissions (`0644` / `0600`) on key authentication and cron files (`/etc/passwd`, `/etc/shadow`, `/etc/gshadow`, `/etc/group`, `/etc/crontab`).
